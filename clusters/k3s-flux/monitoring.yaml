apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: monitoring
  namespace: flux-system
spec:
  interval: 10m
  path: ./infrastructure/monitoring
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  timeout: 5m
  wait: true
  # Temporarily removed hard dependency on infrastructure
  # Will re-enable once Longhorn storage is stable
  # dependsOn:
  #   - name: infrastructure
  #     namespace: flux-system
